# Always use the public npm registry in this repo (CI has no company JFrog access).
registry=https://registry.npmjs.org/
# react-simple-maps@3 declares peer react@16–18; this app uses React 19 and works at runtime.
legacy-peer-deps=true
